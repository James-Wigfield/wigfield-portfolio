# Always include optional (platform-specific) dependencies in the lock file.
# This prevents Cloudflare CI from failing with "npm ci" when native bindings
# (e.g. @emnapi/* from Vite 8/rolldown) are missing from package-lock.json.
include=optional
